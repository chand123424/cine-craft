# Keep these values server-side only.
GROK_API_KEY=
GOOGLE_AI_API_KEY=
HUGGINGFACE_API_KEY=

# mock is deterministic and needs no key. Use gemini, grok, or huggingface in production.
AI_PROVIDER=mock
AI_MODEL=
REQUEST_TIMEOUT_SECONDS=30
AI_MAX_RETRIES=2
